# Codex rules for this project

The canonical rules now live in [`docs/rules/AGENTS.md`](docs/rules/AGENTS.md).
This root file just re-imports them so Codex keeps loading them
automatically (Codex only auto-discovers `AGENTS.md` at the repo root and
in the directory you're working in — a file buried under `docs/` would not be
picked up on its own).

@docs/rules/AGENTS.md
